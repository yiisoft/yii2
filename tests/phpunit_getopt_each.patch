diff --git a/src/Util/Getopt.php b/src/Util/Getopt.php
index ba21be3..6eb7ce5 100644
--- a/src/Util/Getopt.php
+++ b/src/Util/Getopt.php
@@ -35,7 +35,11 @@ class PHPUnit_Util_Getopt
         reset($args);
         array_map('trim', $args);
 
-        while (list($i, $arg) = each($args)) {
+        for (; $arg = current($args), $i = key($args), next($args); ) {
+            if ($arg === false) {
+                break;
+            }
+
             if ($arg == '') {
                 continue;
             }
@@ -94,7 +98,11 @@ class PHPUnit_Util_Getopt
                     if ($i + 1 < $argLen) {
                         $opts[] = array($opt, substr($arg, $i + 1));
                         break;
-                    } elseif (list(, $opt_arg) = each($args)) {
+                    } elseif (
+                        (($opt_arg = current($args) === false) &&
+                            (next($args) && true)) ||
+                        (next($args) && false)
+                    ) {
                     } else {
                         throw new PHPUnit_Framework_Exception(
                             "option requires an argument -- $opt"
@@ -139,8 +147,13 @@ class PHPUnit_Util_Getopt
 
             if (substr($long_opt, -1) == '=') {
                 if (substr($long_opt, -2) != '==') {
-                    if (!strlen($opt_arg) &&
-                        !(list(, $opt_arg) = each($args))) {
+                    if (
+                        !strlen($opt_arg) &&
+                        (
+                            (($opt_arg = current($args) === false) &&
+                                (next($args) && true))
+                            || (next($args) && false)
+                        )) {
                         throw new PHPUnit_Framework_Exception(
                             "option --$opt requires an argument"
                         );
