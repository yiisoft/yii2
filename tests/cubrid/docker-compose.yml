version: '2'
services:

  php:
    build:
      context: ../..
      dockerfile: Dockerfile-cubrid
    ports:
      - 80
    #volumes:
    #  - ../..:/project
    depends_on:
      - cubrid

  cubrid:
    image: lighthopper/cubrid:9.3.6.0002
    command: ./create-start-demodb.sh